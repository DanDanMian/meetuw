const express = require("express");
const router = express.Router();

const User = require("../db/models/user");

//TODO for when authenticated pages check user session
/* router.get("/user", function(req, res) {
    
}); */

module.exports = router;
